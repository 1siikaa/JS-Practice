/*
Variable Lookup:
In JS, javascript engine uses lookup process to find the value of variable associated with it when it is used in your code.
This process involves searching through different scopes to locate the variables declaration.
*/


/*
Lookup Working:
JS uses lexical scoping, meaning the scope of the variable is determined by where it's written in the code.
*/

/*
Lookup Process: 
When a variable is encountered, the engine first checks the current scope. If the variable isn't found here.
*/